spring:
  jpa:
    show-sql: true # 전송 쿼리 확인
    properties:
      hibernate:
        format_sql: true # 전송 쿼리를 format 하여 가독성을 높임
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
  h2:
    console:
      enabled: true
jwt:
  issuer: test@gmail.com
  secret_key: study-spring-boot